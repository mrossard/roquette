twig:
    file_name_pattern: '*.twig'
    globals:
        mercure_topic_prefix: '%env(MERCURE_TOPIC_PREFIX)%'
        vapid_public_key: '%env(VAPID_PUBLIC_KEY)%'

when@test:
    twig:
        strict_variables: true
